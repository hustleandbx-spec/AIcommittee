# 委员会配置文件风格规范

> 本文档定义了架构委员会配置文件的统一风格要求。
> 新项目必须遵循此规范，确保配置文件的一致性和可维护性。

---

## 1. 目录结构

```
<project>/
├── config.yaml                    # 主配置（必需）
├── topics.yaml                    # 议题库（必需）
├── decisions.md                   # 决策日志（可选，运行时生成）
├── shared_context/                # 共享事实层（必需）
│   ├── <fact_1>.md
│   └── <fact_2>.md
├── prompts/                       # 自定义提示词（必需）
│   ├── <node_1>.md
│   ├── <node_2>.md
│   └── converge.md
└── adr/                           # ADR 文档（可选，运行时生成）
    ├── adr_template.md
    └── ADR-*.md
```

---

## 2. 核心设计原则

### 2.1 事实与方法论分离

- **`shared_context/`** 存放**事实**：硬件规格、软件架构、约束条件、已确认决策
- **`prompts/`** 存放**方法论**：如何审查这个领域的设计、审查维度、攻击场景

Agent 不得自行编造事实，只能基于 `shared_context` 辩论。

### 2.2 唯一事实层

每个 `shared_context` 文件必须声明：
```markdown
> 来源：<来源说明>
> 本文件是委员会辩论的**唯一事实层**。Agent 不得自行编造 <具体内容>。
```

### 2.3 标注协议

所有约束必须使用标注协议：
- `[C]` Constraint — 架构硬性约束，违反即设计无效
- `[F]` Formula — 基于已知参数的公式推导值，非实测
- `[R]` Relative — 相对比较值，用于说明改善幅度
- `[M]` Measured — 在指定硬件平台上的实测值

### 2.4 结构化输出

每个审查问题必须包含：
- 编号前缀（`M-/C-/A-/P-`）+ 轮次 + 序号
- 评级（Blocker/Critical/Warning）
- 量化数据（延迟、带宽、内存等）

---

## 3. `config.yaml` 格式规范

### 3.1 文件头

```yaml
# <项目名> 项目配置 — 架构委员会
# 目录结构相对于此文件所在目录 (committee/<project>/)

project_name: "<项目显示名>"
```

### 3.2 段落结构

每个段用 `# ---` 分隔，附带中文注释说明设计意图：

```yaml
# ---------------------------------------------------------------------------
# API 提供商凭证 (凭证本身在 .env, 此处声明环境变量名)
# ---------------------------------------------------------------------------
providers:
  ...

# ---------------------------------------------------------------------------
# 角色模型配置
#
# 设计原则:
#   - <解释为什么需要这些角色，以及领域专家弥补什么盲区>
# ---------------------------------------------------------------------------
roles:
  ...

# ---------------------------------------------------------------------------
# 流水线拓扑
#
# 执行顺序:
#   1. <node_1>  — <说明>
#   2. <node_2>  — <说明>
#   ...
# ---------------------------------------------------------------------------
pipeline:
  ...

# ---------------------------------------------------------------------------
# 共享事实文件 (相对路径)
# 这些是委员会的「唯一事实源」，Agent 不得自行编造
# ---------------------------------------------------------------------------
shared_context:
  ...

# ---------------------------------------------------------------------------
# 自定义提示词 (按节点名覆盖引擎内置默认)
#
# 查找顺序: node_name → type → DEFAULT_PROMPTS
# 领域专家的提示词中包含了其专属的审查维度和场景，
# 这些内容不属于「事实」(不在 shared_context)，
# 而是「方法论」(如何审查这个领域的设计)。
# ---------------------------------------------------------------------------
prompts:
  ...
```

### 3.3 角色设计原则注释

`roles` 段必须解释为什么需要这些角色：

```yaml
roles:
  # 设计原则:
  #   - 软件工程角色 (architect, critic) 负责架构质量
  #   - 领域专家角色 (expert_1, expert_2) 负责垂直领域正确性
  #   - 性能角色 (auditor) 负责运行时量化审计
  #
  # 领域专家的存在是为了弥补纯软件架构师在垂直领域知识上的盲区:
  #   - 架构师可能在软件工程上做对，但在 <领域> 假设上做错
  #   - 架构师可能设计了优雅的抽象，但违反 <领域> 物理约束或任务需求
```

---

## 4. `topics.yaml` 格式规范

### 4.1 文件头

```yaml
# <项目名> 架构委员会 — 议题库
```

### 4.2 议题格式

```yaml
topics:
  <topic_id>:
    topic: |
      审阅 <模块名> 设计 (<源文件>)。
      现有方案: <一句话概述>。

      攻击角度:
      1. <具体技术质疑 1>
      2. <具体技术质疑 2>
      3. ...
    desc: "<简短描述>"
```

**关键点**：
- `topic_id` 使用 snake_case
- `topic` 字段使用 `|` 多行字符串
- 第一行格式固定：`审阅 xxx 设计 (源文件)。`
- 第二行格式固定：`现有方案: 一句话概述。`
- 攻击角度使用编号列表，每个角度是一个具体的技术质疑
- `desc` 是单行简短描述，用于列表显示

---

## 5. `shared_context/` 文件格式规范

### 5.1 文件头

```markdown
# <文档标题>

> 来源：<来源说明>
> 本文件是委员会辩论的**唯一事实层**。Agent 不得自行编造 <具体内容>。
>
> **标注协议** (继承自 ARCHITECTURE.md §1.5):
> - `[C]` Constraint — 架构硬性约束，违反即设计无效
> - `[F]` Formula — 基于已知参数的公式推导值，非实测
> - `[R]` Relative — 相对比较值，用于说明改善幅度
> - `[M]` Measured — 在指定硬件平台上的实测值
```

### 5.2 章节结构

```markdown
---

## 1. <章节标题>

| 列1 | 列2 | 标注 |
|-----|-----|------|
| 值1 | 值2 | [C] |

---

## N. 已确认的设计决策（委员会不得推翻，除非提供新证据）

| # | 决策 | 原因 |
|---|------|------|
| 1 | <决策内容> | <原因，带标注> |

---

## N+1. 已知盲区（委员会应重点关注）

- <盲区 1>
- <盲区 2>
```

**关键点**：
- 必须有 `> 来源:` 头部声明
- 必须有 `> 本文件是委员会辩论的**唯一事实层**` 声明
- 必须定义 `[C]/[F]/[R]/[M]` 标注协议
- 使用 Markdown 表格呈现约束数据，每行带标注
- 必须有"已确认的设计决策"章节（委员会不得推翻）
- 必须有"已知盲区"章节（引导委员会重点关注）

---

## 6. `prompts/` 文件格式规范

### 6.1 共同结构

所有 prompt 文件必须包含以下章节：

```markdown
你是 {project_name} 平台的【{role_desc}】。
<角色身份定义，一句话定位>

## 共享事实层 (唯一真理源)
{shared_context}

<角色专属内容>

## 输出格式

### <前缀>-{round}N: [问题标题] — [Blocker / Critical / Warning]

<输出字段>

## 评级标准
[Blocker]  = <定义>
[Critical] = <定义>
[Warning]  = <定义>
```

### 6.2 Review 类型（领域专家）

```markdown
你是 {project_name} 平台的【{role_desc}】。
你是一名 <领域> 专家。你的唯一任务是审查设计方案的**<审查重点>**，不关心 <不关心的方面>。

## 共享事实层 (唯一真理源)
{shared_context}

## 领域知识基线

以下是你作为领域专家应具备的知识背景，审查时应基于这些认知进行判断。

### 1. <知识板块 1>
<详细的技术背景知识，帮助模型理解领域约束>

### 2. <知识板块 2>
<详细的技术背景知识>

## 审查维度

### 1. <维度 1>
- <审查点>
- <审查点>

### 2. <维度 2>
- <审查点>

## 输出格式

### <前缀>-{round}N: [问题标题] — [Blocker / Critical / Warning]

- **问题推演**: 此设计在哪个场景下会产生错误
- **根因**: 哪一步的假设是错误的
- **领域视角**: 从实际应用角度，此问题的影响
- **修正建议**: 正确的做法是什么

## 评级标准
[Blocker]  = 不改则必然错误
[Critical] = 不改则严重退化
[Warning]  = 潜在风险
```

### 6.3 Attack 类型（安全/反驳专家）

```markdown
你是 {project_name} 平台的【{role_desc}】。
你是一名 <角色>，你的唯一任务是粉碎温床，找出所有设计缺陷。严禁赞美。
你不是在做 code review，你是在模拟系统在真实 <场景> 中崩溃的全过程——从用户可见的故障现象倒推到设计根因。

## 共享事实层 (唯一真理源)
{shared_context}

## 攻击哲学

设计缺陷分为三类，你的攻击必须覆盖全部：

**逻辑缺陷** — 代码/架构层面的错误，可以通过静态分析发现
**领域错配** — 软件工程上正确，但在 <领域> 场景下物理上不成立
**任务脱节** — 系统能跑，但在实际任务中不可用或不安全

## 攻击方法: 崩溃倒推法

从以下 <项目> 专属场景倒推，找到当前设计的根因:

### 场景 A — <场景名>
<场景描述，从用户可见的崩溃现象开始>

**延伸攻击:**
- <延伸攻击点 1>
- <延伸攻击点 2>

### 场景 B — <场景名>
<场景描述>

## 输出格式
每个漏洞必须包含:

### A-{round}N: [漏洞标题] — [Blocker / Critical / Warning]

- **崩溃推演**: 从用户可见的故障现象 → 逐步向代码上游推导 → 定位到设计的哪一处
- **受影响的设计**: 引用前面评审方的具体章节或声明
- **根因**: 为什么这个设计会导致上述故障
- **领域视角**: 从实际应用角度，此故障的影响
- **修正建议**: 如果当前轮次是第 N 轮且之前已提过此漏洞，注明是否已修正

## 评级标准
[Blocker]  = 不改则系统必然崩溃或核心功能无法工作
[Critical] = 不改则性能/可维护性严重退化，或在特定条件下崩溃
[Warning]  = 潜在风险，当前条件下不会触发但设计不够健壮
```

### 6.4 Audit 类型（性能审计师）

```markdown
你是 {project_name} 平台的【{role_desc}】。
你是一名 <角色>，审查设计如同编译器优化 pass——精确、量化、无情。
你审查的不是"这个设计好不好"，而是"这个设计在目标硬件上 <量化问题>"。

## 共享事实层 (唯一真理源)
{shared_context}

## 审计哲学

<场景>下的性能审计与 <对比场景> 完全不同：

**<原则 1> > <原则 2>:**
- <解释为什么这个原则更重要>

## 审计维度

### 1. <维度 1>
<详细说明，包含具体的量化指标>

### 2. <维度 2>
<详细说明>

## 硬性否决条件 (违反任一条即当场否决)

1. **<条件 1>** — <解释>
2. **<条件 2>** — <解释>

## 输出格式
每个性能问题:

### P-{round}N: [问题标题]

- **量化影响**: 延迟增量 X ms | 带宽增量 Y MB/s | 内存增量 Z KB
- **触发条件**: 在哪个精确场景下会触发
- **与硬性约束的关系**: 是否违反否决条件
- **全链路影响**: 此问题对端到端延迟预算的影响
- **修复收益估算**: 修复后预期改善多少

你必须在报告中给出具体的数字估算，不接受纯定性描述。
对于无法精确估算的指标，给出合理范围并标注置信度。
```

### 6.5 Converge 类型（汇总）

```markdown
你是 {project_name} 平台的【{role_desc}】。整场 {round} 轮辩论**已经彻底结束**。
你现在不是辩论参与者，而是**会议记录员**。
严禁继续辩论、严禁提出新观点、严禁反驳任何之前的发言。

## 你的唯一任务
基于上面的全部辩论历史，生成两份产物。

## 产物一: 决策摘要 (纯文本)

用 3-5 句话描述最终决策结论。格式:

结论: <一句话结论>
涉及文件: <逗号分隔的文件列表>

## 产物二: 结构化行动项

**必须**在 ```json 代码块中输出:

```json
{{
  "action_items": [
    {{
      "id": "AI-001",
      "priority": "blocker",
      "file": "path/to/file",
      "action": "具体做什么",
      "detail": "为什么这样做",
      "verification": "如何验证完成"
    }}
  ]
}}
```

## 重要提醒
- 不要再辩论任何内容
- JSON 代码块必须可被 json.loads 直接解析
- 如果没有行动项, 输出空数组 []
- 决策摘要和 JSON 之间用 --- 分隔线隔开

## 共享事实层
{shared_context}
```

---

## 7. 快速生成清单

新项目配置委员会时，按以下顺序创建文件：

- [ ] 创建目录结构：`shared_context/`, `prompts/`, `adr/`
- [ ] 编写 `config.yaml`：providers → roles → pipeline → shared_context → prompts
- [ ] 编写 `topics.yaml`：定义议题，每个议题包含攻击角度
- [ ] 编写 `shared_context/<fact_1>.md`：硬件/架构约束，带标注协议
- [ ] 编写 `shared_context/<fact_2>.md`：软件/流程约束，带标注协议
- [ ] 编写 `prompts/architect_review.md`：架构师评审提示词
- [ ] 编写 `prompts/<expert_1>_review.md`：领域专家 1 提示词
- [ ] 编写 `prompts/<expert_2>_review.md`：领域专家 2 提示词
- [ ] 编写 `prompts/<critic>_attack.md`：反驳专家提示词
- [ ] 编写 `prompts/<auditor>_audit.md`：性能审计师提示词
- [ ] 编写 `prompts/converge.md`：汇总提示词

---

## 8. 参考项目

- `odt/` — 光电吊舱目标检测系统（标准参考）
- `uav-fc/` — 无人机飞控系统

---

## 附录: 模板变量

以下变量由引擎自动注入，可在 prompt 文件中使用：

| 变量 | 来源 | 说明 |
|------|------|------|
| `{project_name}` | config.yaml | 项目显示名 |
| `{role_desc}` | config.yaml roles | 角色描述 |
| `{shared_context}` | shared_context/*.md | 所有事实文件拼接 |
| `{current_topic}` | topics.yaml | 当前议题内容 |
| `{revision_context}` | 引擎生成 | 修正上下文（如有） |
| `{round}` | 引擎生成 | 当前轮次 |
| `{attack_scenarios}` | config.yaml | 攻击场景（仅 attack 类型） |
| `{audit_dimensions}` | config.yaml | 审计维度（仅 audit 类型） |

---

## 9. 委员会初始化工作流

### 9.1 概述

从零开始创建委员会配置的流程：

```
需求书/参考文件 → AI 提取 → .committee.yaml → 用户确认 → generate.py → 委员会配置
```

**核心设计**：
- `.committee.yaml` 是"信息收集"和"配置生成"的**边界**
- AI 负责语义理解和信息提取
- `generate.py` 负责机械转换
- 用户在边界处确认和补充

### 9.2 工具分工

| 工具 | 职责 | 输入 | 输出 |
|------|------|------|------|
| **Claude Code** | 语义理解、信息提取、多轮对话 | 需求书 + 用户对话 | `.committee.yaml` |
| **generate.py** | 机械转换、模板套用 | `.committee.yaml` | 配置文件 |
| **committee** | 辩论执行 | 配置文件 + 议题 | 辩论结果 |

### 9.3 完整流程

#### 阶段 1: AI 提取（对话）

```
用户: 我有个新项目，需求书在 docs/req.md，帮我初始化委员会

Claude:
1. 读取需求书
2. 提取关键信息
3. 生成 .committee.yaml（带 [待确认] [待补充] 标记）
4. 列出待确认项，逐项询问
```

#### 阶段 2: 用户确认（对话）

```
Claude: 硬件平台推断为 "Jetson Orin NX"，是否正确？
用户: 正确
Claude: 已更新，删除 [待确认] 标记

Claude: 需要哪些领域专家？当前建议：
       1. CV/MOT 算法专家
       2. 吊舱部署专家
       是否需要调整？
用户: 只需要 CV 专家
Claude: 已更新，删除 deployment_expert
```

#### 阶段 3: 脚本生成（确定）

```bash
python committee/tools/generate.py .committee.yaml
```

输出：
```
============================================================
  Committee config generation completed
============================================================

  Output directory: .committee
  Generated files: 7

  [OK] Generated:
    - config.yaml
    - topics.yaml
    - prompts/architect_review.md
    - prompts/cv_expert_review.md
    - prompts/critic_attack.md
    - prompts/auditor_audit.md
    - prompts/converge.md

  [WARN] Pending tags:
    - [待确认]: 2
    - [待补充]: 3

============================================================
```

#### 阶段 4: 委员会运行（迭代）

```bash
committee run --topic decoder_registry
```

如发现信息缺失：
1. 补充 `.committee.yaml`
2. 重新运行 `generate.py`
3. 继续委员会运行

### 9.4 标记系统

`.committee.yaml` 和生成的配置文件中使用统一的标记：

| 标记 | 含义 | 处理方式 |
|------|------|---------|
| `[待确认]` | AI 推断，需要用户确认 | 用户确认后删除标记 |
| `[待补充]` | 信息缺失，需要用户提供 | 用户补充后删除标记 |
| `[待讨论]` | 有多种可能，需要委员会讨论 | 委员会讨论后更新 |
| `???` | 完全未知 | 需要用户提供 |

### 9.5 迭代完善

```
                    ┌─────────────────────────────────────┐
                    │            需求书/参考文件           │
                    └─────────────────────────────────────┘
                                      ↓
                    ┌─────────────────────────────────────┐
         第 1 轮 →  │  生成骨架配置（大部分 [待补充]）     │
                    └─────────────────────────────────────┘
                                      ↓
                    ┌─────────────────────────────────────┐
         第 2 轮 →  │  用户补充关键信息，重新生成           │
                    │  （部分 [待确认]，少量 [待补充]）     │
                    └─────────────────────────────────────┘
                                      ↓
                    ┌─────────────────────────────────────┐
         第 3 轮 →  │  委员会运行，发现问题，补充           │
                    │  （大部分确认，少量 [待讨论]）        │
                    └─────────────────────────────────────┘
                                      ↓
                    ┌─────────────────────────────────────┐
         第 N 轮 →  │  配置完善，标记全部清除               │
                    └─────────────────────────────────────┘
```

### 9.6 手动修改检测

`generate.py` 会记录生成文件的哈希值（`.generated_hashes.yaml`），用于检测手动修改：

- **首次生成**：覆盖所有文件
- **再次生成**：
  - 未手动修改的文件 → 覆盖更新
  - 已手动修改的文件 → 跳过，打印警告

这允许用户在生成后手动优化某些文件（如 `shared_context/`），而不会被后续生成覆盖。

---

## 10. 文件清单

### 10.1 模板目录

```
committee/
├── COMMITTEE_TEMPLATE.md              # 本文档
├── tools/
│   ├── README.md                      # 工具使用说明
│   ├── generate.py                    # 配置生成脚本
│   └── METADATA_SPEC.md               # 元数据格式规范
└── template/
    ├── README.md                      # 模板使用说明
    ├── .committee.yaml.example        # 元数据示例
    ├── config.yaml                    # 配置模板
    ├── topics.yaml                    # 议题模板
    ├── shared_context/
    │   ├── architecture.md            # 架构模板
    │   └── constraints.md             # 约束模板
    ├── prompts/
    │   ├── architect_review.md        # 架构师提示词模板
    │   ├── expert_review.md           # 领域专家提示词模板
    │   ├── critic_attack.md           # 反驳专家提示词模板
    │   ├── auditor_audit.md           # 性能审计师提示词模板
    │   └── converge.md                # 汇总提示词模板
    └── adr/
        └── adr_template.md            # ADR 模板
```

### 10.2 项目目录（生成后）

```
<project>/
├── .committee.yaml                    # 项目元数据（AI 填充 + 用户确认）
├── .committee/                        # 生成的委员会配置
│   ├── config.yaml
│   ├── topics.yaml
│   ├── shared_context/
│   │   ├── architecture.md
│   │   └── constraints.md
│   ├── prompts/
│   │   ├── architect_review.md
│   │   ├── <expert>_review.md
│   │   ├── critic_attack.md
│   │   ├── auditor_audit.md
│   │   └── converge.md
│   └── .generated_hashes.yaml         # 哈希记录（用于检测手动修改）
└── ...
```
